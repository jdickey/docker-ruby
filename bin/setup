#!/bin/bash

# This works for MRI; it may or may not work for other Rubies.
RUBY_VERSION=`ruby --version | cut -d ' ' -f 2 | sed 's/p.*//'`

rbenv gemset delete $RUBY_VERSION ./tmp/gemset
rm -rf tmp/gemset .rbenv-gemsets
mkdir -p tmp/gemset
rbenv gemset create $RUBY_VERSION ./tmp/gemset
echo ./tmp/gemset > .rbenv-gemsets
rbenv rehash
gem install yard
yard config --gem-install-yri
gem install docker-api minitest pry-byebug
rbenv rehash
